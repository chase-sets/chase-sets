interface:
  display_name: "Bounded Context Delivery"
  short_description: "Deliver changes with bounded-context evidence"
  default_prompt: "Use $bounded-context-delivery to plan this change in an embedded worktree, ask one blocking question at a time, record the dated plan, create the implementation goal, and require PR/CI/merge/deployment verification plus local cleanup before completion."
