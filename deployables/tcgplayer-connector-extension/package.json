{
  "name": "@chase-sets/app-tcgplayer-connector-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc -p ./tsconfig.json --noEmit",
    "test": "vitest run --config ./vitest.config.ts",
    "test:watch": "vitest --config ./vitest.config.ts",
    "test:chromium": "vite build && playwright test --config ./playwright.config.ts"
  },
  "dependencies": {
    "@chase-sets/channels": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/chrome": "^0.1.36",
    "@types/node": "^25.3.3",
    "typescript": "^7.0.0",
    "vite": "^8.0.0",
    "vitest": "^4.0.18"
  }
}
