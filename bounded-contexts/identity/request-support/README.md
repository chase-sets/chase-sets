`request-support` is the shared HTTP request contract used by multiple Identity routes to keep authorization extraction and request client behavior consistent; files here must represent cross-slice transport concerns rather than business behavior. For example, `permissions.ts` centralizes actor permission guards consumed by route handlers, while `api-client.ts` defines request-scoped API client behavior reused by admin and marketplace route entrypoints.
