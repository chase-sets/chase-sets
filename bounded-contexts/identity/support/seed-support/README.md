`seed-support` contains cross-slice deterministic seed helpers used to populate Identity fixtures in development and tests, with the contract that helpers remain stable and infrastructure-agnostic except for ID generation strategy. For example, `ids.ts` provides repeatable entity identifiers consumed by seed flows that create accounts, users, memberships, invitations, and API keys together.
