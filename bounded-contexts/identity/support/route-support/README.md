`route-support` owns cross-slice route composition primitives for Identity so deployable-facing route modules can share request typing and adapter wiring without duplicating boilerplate; anything here should be route-layer glue, not slice domain logic. For example, `identity-request.ts` defines common request context shaping used by multiple `routes/admin/*` and `routes/marketplace/*` handlers.
