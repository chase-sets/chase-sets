# Email Operations

Chase Sets uses Amazon SES for transactional email. Source bounded contexts publish facts and enqueue provider-agnostic messages; provider delivery is composed by `platform-worker` through `infrastructure/ses-email`.

## Sending Environments

| Environment | From address | SES identity | MAIL FROM domain | Configuration set |
| --- | --- | --- | --- | --- |
| Preview | `notifications@preview.chasesets.com` | `preview.chasesets.com` | `bounce.preview.chasesets.com` | `transactional-preview` |
| Staging | `notifications@staging.chasesets.com` | `staging.chasesets.com` | `bounce.staging.chasesets.com` | `transactional-staging` |
| Production | `notifications@chasesets.com` | `chasesets.com` | `bounce.chasesets.com` | `transactional-production` |

Required worker environment:

```env
NOTIFICATION_EMAIL_PROVIDER=amazon-ses
SES_AWS_REGION=us-east-2
SES_FROM_EMAIL=<environment sender>
SES_CONFIGURATION_SET_NAME=<environment configuration set>
SES_SOURCE_ARN=<environment SES identity ARN>
```

The worker refuses to start with `NOTIFICATION_EMAIL_PROVIDER=amazon-ses` unless all four `SES_*` values are present.

## DNS Requirements

Each SES identity needs:

- Easy DKIM CNAME records generated by SES.
- A custom MAIL FROM MX record pointing at the region-specific SES feedback SMTP host.
- A custom MAIL FROM TXT record: `v=spf1 include:amazonses.com ~all`.
- A DMARC TXT record. Start with `v=DMARC1; p=none;` while observing delivery, then tighten only after bounce and complaint handling is proven.

DigitalOcean DNS hosts `chasesets.com`; do not enable Route 53 automatic publishing for these identities.

`staging.chasesets.com` is also the staging marketplace root and must remain a CNAME to the staging App Platform ingress. Do not add exact-name MX, TXT, A, or AAAA records at `staging.chasesets.com`; staging email identity records must stay on child names such as SES DKIM, `_dmarc.staging.chasesets.com`, and `bounce.staging.chasesets.com`.

## Operational Posture

- Use SES for transactional email first: magic links, account security, order/payment, fulfillment, refund, and payout notices.
- Keep marketing/lifecycle email separate until preference, unsubscribe, segmentation, and campaign operations are ready.
- Keep engagement tracking disabled for security and commerce-critical transactional email unless a later policy explicitly allows it.
- Use Virtual Deliverability Manager and optimized shared delivery in shared environments.
- Do not start with dedicated IPs. Revisit only after steady, clean volume justifies separate IP reputation management.
- Do not use SES Tenant Management until Chase Sets sends on behalf of distinct seller brands or business units.

## Verification

Before enabling SES in a shared environment:

1. Confirm the SES domain identity is verified.
2. Confirm DKIM, custom MAIL FROM, SPF, and DMARC records resolve from authoritative DNS.
3. Confirm GitHub Environment variables/secrets exist for that environment.
4. Confirm Terraform passes the SES values into the `platform-worker` component.
5. Deploy and verify `platform-worker` health is `ACTIVE`.
6. Send a controlled transactional message from the environment and confirm the outbox row is marked sent with provider `amazon-ses`.

Provider bounces, complaints, and delivery notifications should flow through SES/SNS into the provider webhook inbox path before tightening DMARC policy or broadening email volume.
