This is a greenfield project. Breaking changes are encouraged when they reduce entropy and improve clarity. System is horizontally scalable, maintainable, event-sourced, event-driven, follows domain driven design principles, prioritizes clarity and simplicity, uses vertical slices, favors single responsibility, composition over inheritance, functional patterns, and is self-explanatory. Actively remove legacy patterns. Always accept refactors for the optimal solution. The design system is the canonical source of truth for all UI components and patterns with no custom overrides. Treat bounded contexts as the canonical home for behavior, read models, UI, and tests; treat deployables as thin composition roots only. Keep shared folders tiny and explicit. Anything used by only one slice should live in that slice; anything used across contexts should live in contracts/infrastructure, not a deployable. The marketplace is designed to scale and accommodate a wide range of users, accounts are encouraged to both buy and sell, with a focus on competitive pricing, efficient workflows, and better margins for products. Terms use natural language and match both internal and external uses. Bounded contexts use ubiquitous language. Pressure test all solutions. Always review alternatives, tradeoffs, edge cases, and choose the optimal solution.

The default inner loop is watch mode: iterate with `pnpm --filter @chase-sets/<workspace> run test:watch` for sub-second re-runs, and use scoped checks (`pnpm --filter <workspace> run test` and named focused tests for changed script tooling) before opening a PR. On concurrent-lane hosts, set `CHASE_SETS_LANE_MODE=1` for local Vitest commands so the shared config uses lane-safe timeouts and capped workers; leave it unset for the strict default and hosted CI profile. Run `pnpm run verify:static:scoped` before every push. Hosted CI owns the full `pnpm run verify:static`, full `pnpm run verify`, and complete `pnpm run test:scripts` gates.
