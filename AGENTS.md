This is a greenfield project. Breaking changes are encouraged when they reduce entropy and improve clarity. System is horizontally scalable, maintainable, event-sourced, event-driven, follows domain driven design principles, prioritizes clarity and simplicity, uses vertical slices, favors single responsibility, composition over inheritance, functional patterns, and is self-explanatory. The design system is the canonical source of truth for all UI components and patterns with no custom overrides. Treat bounded contexts as the canonical home for behavior, read models, UI, and tests; treat deployables as thin composition roots only. Keep shared folders tiny and explicit. Anything used by only one slice should live in that slice; anything used across contexts should live in contracts/infrastructure, not a deployable. The marketplace is designed to scale and accommodate a wide range of buyers and sellers, with a focus on competitive pricing, efficient workflows, and better margins for low-value cards. Terms use natural language and match both internal and external uses. Bounded contexts use ubiquitous language. Pressure test all solutions.
