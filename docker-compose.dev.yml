services:
  postgres:
    container_name: chase-sets-postgres-dev
    image: pgvector/pgvector:pg16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: catalog
      POSTGRES_PASSWORD: catalog
      POSTGRES_DB: catalog
    volumes:
      - chase-sets-postgres-dev-data:/var/lib/postgresql/data

volumes:
  chase-sets-postgres-dev-data:
