// A canonical constructor spelling that is only ever an ordinary value and is
// never used as a key. The census stays silent by design rather than reporting
// every string that happens to look like a constructor.
export const censusUnrelatedText = "authorizeConsentForActor";
export const censusUnrelatedLabel = `authorizeConsentForProvisioning`;
