// The template form of the same shadowing: the substitution resolves to a
// canonical constructor spelling only under nearest-scope resolution.
const censusShadowedTemplatePart = "unrelatedRuntime";

export function censusShadowedTemplateProbe() {
  const censusShadowedTemplatePart = "authorizeConsent";
  return authorization[`${censusShadowedTemplatePart}ForActor`](context);
}
