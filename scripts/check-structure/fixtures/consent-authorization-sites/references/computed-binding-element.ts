import { "authorizeConsentForActor" as importedAlias } from "./authority";
export { "authorizeConsentForActor" as exportedAlias } from "./authority";

// prettier-ignore
const { "authorizeConsentForActor": consentAlias } = authorization;
consentAlias(context);

// prettier-ignore
function parameterProbe({ "authorizeConsentForActor": parameterAlias }) {
  parameterAlias(context);
}

const { ["authorizeConsentForActor"]: computedAlias } = authorization;
computedAlias(context);
