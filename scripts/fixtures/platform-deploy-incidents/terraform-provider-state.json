{
  "name": "Terraform provider state lock failure",
  "input": {
    "phase": "staging-deploy",
    "diagnostics": [
      {
        "operation": "terraform apply",
        "error": "Error acquiring the state lock from the remote backend"
      }
    ]
  },
  "expected": {
    "rootCauseCode": "terraform-provider-or-state",
    "affectedComponent": "terraform",
    "phase": "terraform",
    "blocking": true
  }
}
