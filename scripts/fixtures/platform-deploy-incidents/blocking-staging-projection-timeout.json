{
  "name": "blocking staging projection convergence timeout",
  "input": {
    "phase": "staging-verification",
    "diagnostics": [
      {
        "outcome": "timeout",
        "reason": "Projection convergence checkpoint timed out before the blocking verification completed"
      }
    ]
  },
  "expected": {
    "rootCauseCode": "blocking-staging-verification",
    "affectedComponent": "staging-verification",
    "phase": "staging-verification",
    "blocking": true
  }
}
